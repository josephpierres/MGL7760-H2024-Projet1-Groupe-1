apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: bbl
  name: mysql-pvc
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
