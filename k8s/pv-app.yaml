apiVersion: v1
kind: PersistentVolume
metadata:
  name: app-pv
  namespace: bbl
spec:
  storageClassName: app-sc
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 3Gi
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /var/www/html
