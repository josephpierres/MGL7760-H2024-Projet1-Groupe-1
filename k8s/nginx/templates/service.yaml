---
apiVersion: v1
kind: Service
metadata:
  name: flask-nginx
  namespace: bbl
  labels:
    app: flaskapp
spec:
  type: LoadBalancer
  selector:
    app: flaskapp
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http