apiVersion: v1
kind: Service
metadata:
  name: flask-redis
  namespace: bbl
  labels:
    app: flaskapp
    role: master
    tier: backend
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: flaskapp
    role: master
    tier: backend
  clusterIP: None